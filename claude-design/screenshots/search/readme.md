12 Dawn · Predictive search + Search results
Search

Two surfaces: the dropdown that appears as a shopper types in the header (suggestions + product matches + collections), and the /search results page itself.